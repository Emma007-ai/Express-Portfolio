<!doctype html>
<html>
<head>
  <title>Error — <%= message %></title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
  <h1>Error</h1>
  <h2><%= message %></h2>

  <% if (error && error.status) { %>
    <p>Status: <%= error.status %></p>
  <% } %>

  <% if (error && error.stack) { %>
    <pre><%= error.stack %></pre>
  <% } %>

  <p>Site owner: Emmanuel Uzoma-Akonye</p>
</body>
</html>
